POSTGRES_USER=jacaero
POSTGRES_PASSWORD=change-me
POSTGRES_DB=jacaero
VITE_API_URL=http://localhost:4000
# Host path bind-mounted read-only into the backend at /docs (see DOCS_ROOT_PATH in apps/backend/.env).
# Leave unset for local dev — defaults to an empty ./local-docs folder.
DOCS_HOST_PATH=
